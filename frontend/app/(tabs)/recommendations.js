import { ThemedText, ThemedView } from "../../components";

export default function RecommendationsScreen() {
  return (
    <ThemedView
      style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
    >
      <ThemedText>Recommendations Screen</ThemedText>
    </ThemedView>
  );
  ``;
}
