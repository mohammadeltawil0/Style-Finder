1. Start the backend
   cd server
   ./mvnw spring-boot:run

2. Start the frontend
   Install dependencies
       cd frontend
       npm install
Start the app
       npm run start
       Select "w" to start the web application
